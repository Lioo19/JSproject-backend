/**
 * Test for reports
 */
"use strict";

/* global describe it */

var assert = require("assert");

describe("We want to test", function() {
    it("It should have an assert", function() {
        assert.equal(true, true);
    });
});
